---
phase: 06-scoring
plan: 02
type: execute
---

<objective>
Update the HUD overlay and game over screen to display score, combo, and best score instead of raw survival time.

Purpose: Make the scoring system visible to the player, providing real-time feedback that drives engagement and the "one more try" loop.
Output: HUD showing live score + combo indicator, game over screen showing final score with best score comparison.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-scoring/06-01-SUMMARY.md

# Key source files:
@lib/screens/hud_overlay.dart
@lib/screens/game_over_screen.dart
@lib/game/pulse_game.dart
@lib/game/managers/score_manager.dart
@lib/game/config/game_config.dart

**Established patterns:** StatefulWidget with Timer.periodic for HUD refresh, SafeArea + Stack + Positioned layout, GameConfig.textColor, withValues(alpha:) for opacity
**Constraining decisions:**
- Phase 02-05: HUD uses StatefulWidget with 33ms refresh timer for live updates
- Phase 02-04: Game over uses full-screen GestureDetector for tap-to-retry, menu button bottom-center
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update HUD overlay with score and combo display</name>
  <files>lib/screens/hud_overlay.dart</files>
  <action>
Replace the survival time display with score-focused HUD:

1. **Score display** (top center, where timer was): Show `displayScore` as large number. Format with no decimal — just the integer score. Keep the same text style (white, 28px, bold, letterSpacing 2).

2. **Combo indicator** (below score, only visible when combo > 1): Show "×{comboMultiplier.toStringAsFixed(1)}" in accent color (use GameConfig.playerColor for energy). Slightly smaller font (20px). Use AnimatedOpacity or simple conditional visibility — show when combo > 1, hide when 0-1.

3. **Score event flash** (below combo area): When ScoreManager.lastScoreEvent is non-null, show the event text (e.g., "DODGE +25") in a smaller font (14px), slightly transparent. Fades naturally as lastScoreEvent clears after 0.8s.

4. **Keep pause button** in top-right corner as-is.

Access score data via `widget.game.scoreManager.displayScore`, `.combo`, `.comboMultiplier`, `.lastScoreEvent`.

Do NOT add best score to HUD yet — that comes after persistence in plan 06-03.
  </action>
  <verify>Run `C:/flutter/bin/flutter analyze` — no errors in hud_overlay.dart</verify>
  <done>HUD shows live score (integer), combo multiplier when active, and score event flashes on dodge/near-miss</done>
</task>

<task type="auto">
  <name>Task 2: Update game over screen with score summary</name>
  <files>lib/screens/game_over_screen.dart</files>
  <action>
Replace survival time with score summary on game over:

1. **Final score** (large, where time was): Show `game.scoreManager.displayScore` as prominent number. White, 40px, bold.

2. **Score label** (small, above the number): "SCORE" in 14px, slightly transparent text.

3. **Stats row** (below score, before "TAP TO RETRY"): Show survival time + combo info in a compact row:
   - "{survivalTime.toStringAsFixed(1)}s" — time survived
   - "Best ×{bestCombo}" — highest combo (show only if bestCombo > 1)
   Use smaller font (16px), slightly transparent, with a divider dot between stats.

4. Keep "TAP TO RETRY" and "MENU" button unchanged.

5. Keep the full-screen tap for retry and the semi-transparent overlay.

Layout order top to bottom: "GAME OVER" → "SCORE" label → score number → stats row → "TAP TO RETRY" → (bottom) "MENU"
  </action>
  <verify>Run `C:/flutter/bin/flutter analyze` — no errors. Run `C:/flutter/bin/flutter build web` to confirm.</verify>
  <done>Game over screen shows final score prominently, survival time and best combo as secondary stats</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze` passes with no errors
- [ ] `C:/flutter/bin/flutter build web` succeeds
- [ ] HUD displays live score that increases during gameplay
- [ ] Combo multiplier appears when combo > 1
- [ ] Score events flash briefly on dodge and near-miss
- [ ] Game over screen shows final score and stats
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Score is prominently visible during gameplay and on death
- Combo system provides real-time feedback
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring/06-02-SUMMARY.md`
</output>
