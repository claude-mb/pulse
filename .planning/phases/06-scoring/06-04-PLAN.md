---
phase: 06-scoring
plan: 04
type: execute
---

<objective>
Create a celebratory new high score moment with audio fanfare and visual effects when the player beats their best score.

Purpose: Make beating your best score feel like an achievement — the emotional payoff that drives repeated play.
Output: Audio fanfare + visual celebration (golden flash, "NEW BEST!" animation) on new high score detection.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-scoring/06-01-SUMMARY.md
@.planning/phases/06-scoring/06-02-SUMMARY.md
@.planning/phases/06-scoring/06-03-SUMMARY.md

# Key source files:
@lib/game/pulse_game.dart
@lib/game/managers/score_manager.dart
@lib/screens/game_over_screen.dart
@lib/utils/audio_manager.dart
@tool/generate_audio.dart
@lib/game/config/game_config.dart

**Established patterns:** WAV synthesis in tool/generate_audio.dart, AudioManager preloading, FlameAudio.play for one-shot SFX, particle factory pattern, FlashOverlay for full-screen color effects
**Constraining decisions:**
- Phase 05-01: Pure Dart WAV synthesis, AudioPool for frequent SFX, FlameAudio.play for one-shot
- Phase 04-02: Particle factory pattern with static create() method
- Phase 04-03: FlashOverlay pattern at priority 100
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate high score fanfare audio and wire to AudioManager</name>
  <files>tool/generate_audio.dart, lib/utils/audio_manager.dart, lib/game/config/game_config.dart</files>
  <action>
1. Add a `generateHighScoreFanfare()` function to tool/generate_audio.dart:
   - Short celebratory sound (0.6s duration)
   - Rising arpeggio: three notes ascending (C5→E5→G5 or similar, roughly 523→659→784 Hz)
   - Each note ~0.15s with slight overlap
   - Use sine waves with quick attack, medium decay
   - Add slight shimmer/brightness — use harmonic overtones (2× frequency at 30% amplitude)
   - Output to assets/audio/high_score_fanfare.wav

2. Call the new generator in main() alongside existing generators.

3. Run the generator: `cd C:/claude/ClaudesChoice && C:/flutter/bin/dart run tool/generate_audio.dart`

4. Add 'high_score_fanfare.wav' to AudioManager.initialize() preload list.

5. Add to GameConfig: `static const double highScoreFanfareVolume = 0.8;`
  </action>
  <verify>Verify file exists: `ls -la C:/claude/ClaudesChoice/assets/audio/high_score_fanfare.wav`. Run `C:/flutter/bin/flutter analyze`.</verify>
  <done>high_score_fanfare.wav generated, preloaded in AudioManager, ready to play</done>
</task>

<task type="auto">
  <name>Task 2: Add visual celebration and wire fanfare to game over flow</name>
  <files>lib/game/pulse_game.dart, lib/screens/game_over_screen.dart, lib/game/config/game_config.dart</files>
  <action>
1. **PulseGame.gameOver()**: After saving best score, check if it's a new best:
   ```
   final isNewBest = ScoreRepository.instance.isNewBest(currentScore);
   // Save AFTER checking (saveBestScore updates the stored value)
   ScoreRepository.instance.saveBestScore(currentScore);
   if (isNewBest) {
     audioManager.playSfx('high_score_fanfare.wav', volume: GameConfig.highScoreFanfareVolume);
   }
   ```
   Store isNewBest on the game instance so the game over screen can read it:
   - Add `bool lastRunWasNewBest = false;` field to PulseGame
   - Set it in gameOver() after the check
   - Reset to false in startGame() and resetGame()

2. **Game over screen**: Enhance the "NEW BEST!" display when `game.lastRunWasNewBest` is true:
   - Replace the dimmed "BEST {score}" text with "NEW BEST!" in gold/amber color (Color(0xFFFFD700))
   - Add a subtle pulsing animation on the "NEW BEST!" text using AnimatedBuilder or a simple AnimationController with sin wave opacity (0.7 to 1.0)
   - Convert GameOverScreen to StatefulWidget if needed for animation controller

3. Add to GameConfig:
   - `static const Color highScoreColor = Color(0xFFFFD700);` (gold)
  </action>
  <verify>Run `C:/flutter/bin/flutter analyze` — no errors. Run `C:/flutter/bin/flutter build web` to confirm.</verify>
  <done>New high score triggers fanfare audio and golden "NEW BEST!" pulsing text on game over screen</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>New high score celebration with audio fanfare and golden "NEW BEST!" animation on game over screen</what-built>
  <how-to-verify>
    1. Run: `C:/flutter/bin/flutter run -d chrome`
    2. Play a game and die — note your score
    3. Play again and try to beat it
    4. On death with new high score: verify rising fanfare sound plays
    5. On game over screen: verify "NEW BEST!" appears in gold with subtle pulse animation
    6. Play again and die with lower score: verify NO fanfare, shows "BEST {score}" normally
    7. Confirm the celebration feels satisfying but not annoying/over-the-top
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze` passes with no errors
- [ ] `C:/flutter/bin/flutter build web` succeeds
- [ ] Fanfare plays only on new high score
- [ ] "NEW BEST!" visual appears with gold color and pulse animation
- [ ] Normal death (no new best) shows standard game over
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Human verified celebration feels satisfying
- Fanfare audio and visual celebration work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring/06-04-SUMMARY.md`
</output>
