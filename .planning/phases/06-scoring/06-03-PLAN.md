---
phase: 06-scoring
plan: 03
type: execute
---

<objective>
Add local persistence for high scores using shared_preferences, creating a ScoreRepository that saves and loads the player's best score.

Purpose: Persist the best score across sessions so players have a target to beat, reinforcing the "one more try" loop.
Output: ScoreRepository that persists best score, wired into game flow (save on death, load on start, display in HUD and game over).
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-scoring/06-01-SUMMARY.md
@.planning/phases/06-scoring/06-02-SUMMARY.md

# Key source files:
@lib/game/pulse_game.dart
@lib/game/managers/score_manager.dart
@lib/screens/hud_overlay.dart
@lib/screens/game_over_screen.dart
@pubspec.yaml

**Established patterns:** Singleton pattern (AudioManager), async initialize() pattern, GameConfig for constants
**Constraining decisions:**
- PROJECT.md: Local-only data storage, no backend
- Phase 05-01: AudioManager singleton pattern with initialize() + _initialized guard
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared_preferences and create ScoreRepository</name>
  <files>pubspec.yaml, lib/utils/score_repository.dart</files>
  <action>
1. Add `shared_preferences: ^2.3.0` to pubspec.yaml dependencies (after flame_audio). Run `C:/flutter/bin/flutter pub get`.

2. Create ScoreRepository as a singleton (matching AudioManager pattern):
   - `static final ScoreRepository instance = ScoreRepository._();`
   - `ScoreRepository._();`
   - `late SharedPreferences _prefs;`
   - `bool _initialized = false;`

   Methods:
   - `Future<void> initialize()` — get SharedPreferences instance, guard with _initialized
   - `int get bestScore` — read 'best_score' key, default 0
   - `Future<void> saveBestScore(int score)` — write 'best_score' if score > current best
   - `bool isNewBest(int score)` — returns score > bestScore (for celebration detection)

   Keep it simple — just best score for now. Stats come in plan 06-05.
  </action>
  <verify>Run `C:/flutter/bin/flutter pub get` succeeds. Run `C:/flutter/bin/flutter analyze` — no errors.</verify>
  <done>shared_preferences added, ScoreRepository created with bestScore persistence</done>
</task>

<task type="auto">
  <name>Task 2: Wire persistence into game flow and display</name>
  <files>lib/game/pulse_game.dart, lib/game/managers/score_manager.dart, lib/screens/hud_overlay.dart, lib/screens/game_over_screen.dart</files>
  <action>
1. **PulseGame.onLoad()**: Initialize ScoreRepository before game starts:
   `await ScoreRepository.instance.initialize();`
   (Add import for score_repository.dart)

2. **PulseGame.gameOver()**: After setting state to gameOver, check and save best score:
   ```
   final currentScore = scoreManager.displayScore;
   ScoreRepository.instance.saveBestScore(currentScore);
   ```

3. **ScoreManager**: Add a getter for checking new best:
   - `bool get isNewBest => ScoreRepository.instance.isNewBest(displayScore);`
   (This will be used by celebration in plan 06-04)

4. **HUD overlay**: Add best score display in top-left area:
   - Small text "BEST {bestScore}" at top-left, 14px, alpha 0.5
   - Read from `ScoreRepository.instance.bestScore`
   - Only show if bestScore > 0

5. **Game over screen**: Add best score comparison:
   - Below the score number, show "BEST {bestScore}"
   - If current score > best: show "NEW BEST!" in GameConfig.playerColor (this is a preview — full celebration in plan 06-04)
   - If current score <= best: show "BEST {bestScore}" in dimmed text
  </action>
  <verify>Run `C:/flutter/bin/flutter analyze` — no errors. Run `C:/flutter/bin/flutter build web` to confirm compilation.</verify>
  <done>Best score persists across sessions, displays in HUD and game over, new best detected</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze` passes with no errors
- [ ] `C:/flutter/bin/flutter build web` succeeds
- [ ] Best score saves when beating previous best
- [ ] Best score loads on app restart
- [ ] HUD shows best score indicator
- [ ] Game over shows best score comparison
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Best score persists across game sessions
- Players can see their target and know when they beat it
</success_criteria>

<output>
After completion, create `.planning/phases/06-scoring/06-03-SUMMARY.md`
</output>
