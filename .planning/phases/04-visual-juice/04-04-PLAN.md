---
phase: 04-visual-juice
plan: 04
type: execute
---

<objective>
Establish the geometric art style — styled obstacles, ambient background geometry, and player visual polish.

Purpose: Transform the placeholder rectangles and plain background into a cohesive minimalist geometric aesthetic that looks intentional, not prototype.
Output: Styled obstacles with outlines, background grid/geometry, player glow effect.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-juice/04-01-SUMMARY.md
@.planning/phases/04-visual-juice/04-02-SUMMARY.md
@.planning/phases/04-visual-juice/04-03-SUMMARY.md

@lib/game/components/obstacle.dart
@lib/game/components/player.dart
@lib/game/pulse_game.dart
@lib/game/config/game_config.dart

**Tech stack available:** Flutter 3.41.0, Flame ^1.35.0
**Established patterns:** Custom render via canvas.drawPath (Player diamond), RectangleComponent (Obstacle), Paint objects for styling

**Constraining decisions:**
- Player is PolygonComponent (diamond/rhombus) — can add glow/outline via render override
- Obstacles are RectangleComponent — can override render for styled drawing
- backgroundColor 0xFF1A1A2E, obstacleColor 0xFF0F3460, accentColor 0xFF16213E
- Art style is geometric/minimalist — clean lines, no textures, intentional simplicity
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style obstacles with outlines and glow</name>
  <files>lib/game/components/obstacle.dart, lib/game/config/game_config.dart</files>
  <action>
  Override `render()` on Obstacle to draw styled rectangles instead of plain fills.

  Implementation:
  - Keep the filled rectangle (current behavior via RectangleComponent's default render)
  - Override render() to draw manually:
    1. Fill rectangle with obstacleColor (current)
    2. Draw a 1.5px stroke outline in a lighter shade (obstacleColor lightened — e.g., 0xFF1A5276)
    3. Add a subtle inner highlight: a thin line at the top edge in a slightly lighter shade for depth
  - Use `canvas.drawRect()` with different Paint objects for fill, stroke, and highlight

  Add to GameConfig: `obstacleOutlineColor: Color(0xFF1A5276)`, `obstacleHighlightColor: Color(0xFF2471A3)`

  Since we're overriding render(), remove the super.render() call for RectangleComponent (we're drawing everything manually now). Keep the super.onLoad() for the hitbox.

  The effect should make obstacles look like glowing geometric blocks against the dark background.
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub && C:/flutter/bin/flutter build web --no-pub</verify>
  <done>Obstacles render with fill + outline + highlight. Geometric style visible. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add background grid and ambient geometry</name>
  <files>lib/game/components/background.dart, lib/game/pulse_game.dart, lib/game/config/game_config.dart</files>
  <action>
  Create `GameBackground` extending `Component` with custom render — draws a subtle grid pattern and optional floating geometric shapes.

  Implementation:
  - Grid: Vertical and horizontal lines every 50px across the world (400×800), drawn in accentColor (0xFF16213E) with very thin stroke (0.5px) and low opacity (~0.3). This creates a subtle tron-like grid without overwhelming.
  - The grid should slowly scroll downward (same direction as obstacles) at a fraction of obstacle speed (~20% of base speed) for parallax depth feel. Track a `_scrollOffset` that increments in update() and wraps at grid spacing.
  - Priority: -10 (render behind everything)

  Add to GameConfig: `gridSpacing: 50.0`, `gridColor: Color(0xFF16213E)`, `gridOpacity: 0.3`, `gridScrollSpeed: 0.15` (fraction of obstacle speed)

  Add GameBackground to world in PulseGame.onLoad(), before other components. Use `priority: -10` so it renders behind everything.

  Keep the grid minimal and atmospheric — it should add depth without visual noise.
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub && C:/flutter/bin/flutter build web --no-pub</verify>
  <done>Background grid renders with subtle scrolling parallax. Priority correctly behind game objects. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Add player glow and idle pulse</name>
  <files>lib/game/components/player.dart, lib/game/config/game_config.dart</files>
  <action>
  Enhance player rendering with a glow effect and subtle idle pulsing animation.

  Implementation:
  - Override render() on Player (PolygonComponent). Draw the diamond shape manually:
    1. Draw a larger, blurred version first (glow layer): same diamond path but slightly scaled up (1.3x), drawn with playerColor at ~0.3 opacity and MaskFilter.blur(BlurStyle.normal, 8.0) on Paint. This creates a soft glow around the player.
    2. Draw the solid diamond on top (current render, or manually draw the path with playerColor fill)
  - Idle pulse: track a `_pulseTimer` in update(). Use `sin(_pulseTimer * 2 * pi * pulseFrequency)` to oscillate the glow scale between 1.2x and 1.4x, creating a gentle breathing effect. Frequency: ~1.5 Hz (subtle, not distracting).

  Add to GameConfig: `playerGlowRadius: 8.0`, `playerGlowOpacity: 0.3`, `playerPulseFrequency: 1.5`

  The glow should make the player feel alive and draw the eye to it. The pulse should be very subtle — if the player notices it consciously, it's too strong.
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub && C:/flutter/bin/flutter build web --no-pub</verify>
  <done>Player renders with soft glow and subtle idle pulse animation. Build succeeds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze` passes
- [ ] `C:/flutter/bin/flutter build web` succeeds
- [ ] Obstacles have visible outline and highlight
- [ ] Background grid scrolls subtly
- [ ] Player has visible glow and gentle pulse
- [ ] All render priorities correct (background < obstacles/player < effects)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Visual style is cohesive geometric/minimalist
- No performance regression from custom rendering
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-juice/04-04-SUMMARY.md`
</output>
