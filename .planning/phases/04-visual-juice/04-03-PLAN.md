---
phase: 04-visual-juice
plan: 03
type: execute
---

<objective>
Add flash and color feedback effects — death flash overlay and difficulty-responsive danger tint.

Purpose: Color feedback creates subconscious tension (danger tint) and dramatic punctuation (death flash) that deepens the gameplay feel.
Output: Full-screen death flash effect, background danger tint that scales with difficulty.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-juice/04-01-SUMMARY.md
@.planning/phases/04-visual-juice/04-02-SUMMARY.md

@lib/game/pulse_game.dart
@lib/game/components/player.dart
@lib/game/managers/difficulty_manager.dart
@lib/game/config/game_config.dart

**Tech stack available:** Flutter 3.41.0, Flame ^1.35.0
**Established patterns:** Component-based architecture, OpacityEffect for fade-out, DifficultyManager with multipliers

**Constraining decisions:**
- backgroundColor is 0xFF1A1A2E (dark navy) — danger tint should subtly shift warm
- DifficultyManager has 5 levels with time thresholds — tint should scale across these
- Death delays pause by 0.3s (from 04-01) — flash has time to animate
- Combo glow deferred to Phase 6 when scoring/combo system exists
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create death flash overlay effect</name>
  <files>lib/game/effects/flash_overlay.dart, lib/game/pulse_game.dart, lib/game/config/game_config.dart</files>
  <action>
  Create `FlashOverlay` extending `RectangleComponent` — a full-screen white rectangle that flashes on death and rapidly fades out.

  Implementation:
  - Size: GameConfig.worldWidth × GameConfig.worldHeight, position (0,0)
  - Initial opacity: 0.7 (not fully opaque — should feel like a flash, not a blackout)
  - Paint color: white (0xFFFFFFFF)
  - On load: add OpacityEffect.fadeOut with EffectController(duration: 0.25), onComplete: removeFromParent
  - Priority: high (render on top of everything) — set priority to 100 in constructor

  Add to GameConfig: `flashDuration: 0.25`, `flashOpacity: 0.7`

  Wire in PulseGame.gameOver(): add FlashOverlay to the world alongside the death particles and shake. The flash, shake, and particles all fire simultaneously for maximum impact.
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub && C:/flutter/bin/flutter build web --no-pub</verify>
  <done>Death triggers a white flash overlay that fades out in 0.25s. No analysis errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create difficulty danger tint overlay</name>
  <files>lib/game/components/danger_tint.dart, lib/game/pulse_game.dart, lib/game/config/game_config.dart</files>
  <action>
  Create `DangerTint` extending `RectangleComponent` with `HasGameReference<PulseGame>` — a persistent full-screen overlay that subtly shifts color as difficulty increases.

  Implementation:
  - Size: GameConfig.worldWidth × GameConfig.worldHeight, position (0,0)
  - Color: dark red (e.g., 0xFFFF0000) at very low opacity
  - In update(): read `game.difficultyManager.difficultyLevel` (0-4). Map to opacity: level 0 → 0.0 (invisible), level 1 → 0.02, level 2 → 0.05, level 3 → 0.08, level 4 → 0.12. Use lerp for smooth transitions between levels based on difficultyManager's progress within current level.
  - Priority: -1 (render behind obstacles and player, but above background)

  Add to GameConfig: `dangerTintColor: Color(0xFFFF2020)`, `dangerTintMaxOpacity: 0.12`

  Add DangerTint to world in PulseGame.onLoad() (persistent component, always present). It's invisible at difficulty 0 and gradually becomes visible.

  The effect should be VERY subtle — players shouldn't consciously notice it, but should feel increasing tension.
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub && C:/flutter/bin/flutter build web --no-pub</verify>
  <done>Danger tint overlay exists, scales with difficulty level, invisible at start and subtle at max. Build succeeds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze` passes
- [ ] `C:/flutter/bin/flutter build web` succeeds
- [ ] Death flash fires alongside shake and particles
- [ ] Danger tint is invisible at difficulty 0
- [ ] Danger tint subtly visible at high difficulty
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Flash and tint effects are additive with existing shake/particles
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-juice/04-03-SUMMARY.md`
</output>
