---
phase: 04-visual-juice
plan: 05
type: execute
---

<objective>
Add smooth animations for game state transitions — death sequence, game start entrance, and restart flow.

Purpose: Transitions between game states currently feel abrupt (instant pause/reset). Smooth animations create dramatic moments and polish the experience.
Output: Death slow-motion effect, game start entrance animation, smooth restart transition.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-juice/04-01-SUMMARY.md
@.planning/phases/04-visual-juice/04-02-SUMMARY.md
@.planning/phases/04-visual-juice/04-03-SUMMARY.md
@.planning/phases/04-visual-juice/04-04-SUMMARY.md

@lib/game/pulse_game.dart
@lib/game/components/player.dart
@lib/game/config/game_config.dart

**Tech stack available:** Flutter 3.41.0, Flame ^1.35.0
**Established patterns:** MoveEffect + EffectController for animation, Future.delayed for timed sequences, game state machine

**Constraining decisions:**
- gameOver() already delays pause by ~0.3s for shake (from 04-01) — death animation must work within this window or extend it
- resetGame()/startGame() must clear all effects cleanly
- Existing obstacles continue falling during death delay — dramatic effect
- Player position resets immediately on restart — entrance animation happens after reset
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add death sequence animation</name>
  <files>lib/game/pulse_game.dart, lib/game/components/player.dart, lib/game/config/game_config.dart</files>
  <action>
  Enhance the death moment with a brief slow-motion effect and player death animation.

  **Slow motion:** In PulseGame.gameOver(), before the delayed pause, temporarily reduce game speed using `game.timeScale` or by modifying `dt` in update(). Approach: add a `_timeScale` field (default 1.0) to PulseGame. In update(), multiply dt by _timeScale before passing to super.update(). On death, set _timeScale to 0.3 (slow-mo) for 0.3s, then restore to 1.0 and pause. Extend the pre-pause delay from 0.3s to 0.5s to accommodate the slow-mo window. Actually, simpler: Flame's FlameGame has a `timeScale` property concept, but implementing manually is cleaner. Override `update(double dt)` to apply `dt * _timeScale`. Reset _timeScale to 1.0 in startGame()/resetGame().

  **Player death animation:** Add a `playDeathAnimation()` method on Player. When called (from gameOver()):
  - Add a ScaleEffect that shrinks the player to 0 over 0.4s with a curved controller (Curves.easeIn)
  - Add an OpacityEffect that fades to 0 simultaneously (0.4s)
  - These effects run during the slow-mo window, so visually they play out slower (more dramatic)

  Modify gameOver() sequence: set _timeScale = 0.3, trigger shake + flash + particles + player death anim, delay pause by 0.5s (accounting for slow-mo).

  Add to GameConfig: `deathSlowMoScale: 0.3`, `deathSlowMoDuration: 0.5`, `playerDeathAnimDuration: 0.4`

  In resetGame()/startGame(): restore _timeScale = 1.0, reset player scale/opacity to normal (add resetVisuals() on Player that restores scale to Vector2.all(1) and opacity to 1.0).
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub && C:/flutter/bin/flutter build web --no-pub</verify>
  <done>Death triggers slow-motion, player shrinks/fades, then game pauses. Restart restores all visuals. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add game start and restart entrance animations</name>
  <files>lib/game/pulse_game.dart, lib/game/components/player.dart, lib/game/config/game_config.dart</files>
  <action>
  Add entrance animations when starting or restarting the game to replace the instant-appear feel.

  **Player entrance:** Add `playEntranceAnimation()` on Player. When called:
  - Start with scale 0 and opacity 0 at the center position
  - Add ScaleEffect from 0 to 1.0 over 0.3s with Curves.elasticOut for a satisfying pop-in
  - Add OpacityEffect from 0 to 1.0 over 0.2s

  **Spawn protection:** Add a brief invulnerability window during entrance. Add `bool _invulnerable = false` to Player. Set true during entrance, set false after 0.5s (via Future.delayed or a timer). In onCollisionStart(), add guard: `if (_invulnerable) return;`. Reset _invulnerable to false in resetPosition().

  Wire in PulseGame: in startGame() and resetGame(), after resetting position, call `player.playEntranceAnimation()`. The obstacle spawner already waits for its timer to tick, so there's a natural delay before first obstacle appears.

  Add to GameConfig: `entranceAnimDuration: 0.3`, `entranceInvulnerabilityDuration: 0.5`

  Keep it fast — the player should feel the "pop in" but not wait around. The zero-friction restart loop must stay snappy.
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub && C:/flutter/bin/flutter build web --no-pub</verify>
  <done>Game start shows player pop-in with brief invulnerability. Restart is smooth with entrance animation. Build succeeds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze` passes
- [ ] `C:/flutter/bin/flutter build web` succeeds
- [ ] Death sequence: slow-mo + shrink/fade + pause
- [ ] Start/restart: player pops in with brief invulnerability
- [ ] _timeScale resets properly on restart
- [ ] No collision bugs during entrance invulnerability
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Death feels dramatic, restart feels snappy
- Zero-friction retry loop preserved (entrance is fast, < 0.5s)
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-juice/04-05-SUMMARY.md`
</output>
