---
phase: 05-audio-system
plan: 04
type: execute
---

<objective>
Add difficulty-reactive audio behavior so the soundscape intensifies as the game gets harder.

Purpose: Audio should mirror the visual danger tint and speed escalation — subtle at low difficulty, more intense and urgent at high difficulty. This creates subconscious tension that matches the gameplay pressure.
Output: SFX volume/behavior scales with difficulty, spawn audio syncs with visual pulse.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-audio-system/05-01-SUMMARY.md
@.planning/phases/05-audio-system/05-02-SUMMARY.md
@.planning/phases/05-audio-system/05-03-SUMMARY.md

# Key source files:
@lib/utils/audio_manager.dart
@lib/game/pulse_game.dart
@lib/game/managers/difficulty_manager.dart
@lib/game/managers/obstacle_spawner.dart
@lib/game/components/obstacle.dart
@lib/game/config/game_config.dart

**Tech stack available:** flame_audio, AudioManager with pools, DifficultyManager with speedMultiplier/difficultyLevel
**Established patterns:** DifficultyManager provides speedMultiplier (1.0-1.6) and difficultyLevel (1-5), danger tint opacity scales with speedMultiplier
**Constraining decisions:**
- [03-03]: Smooth lerp between difficulty thresholds, speed primary escalation
- [04-03]: Danger tint opacity 0.0/0.02/0.05/0.08/0.12 across 5 levels — audio should mirror this subtlety
- [04-06]: Spawn cue already tied to backgroundPulse.kick() in ObstacleSpawner
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scale SFX volume and intensity with difficulty</name>
  <files>lib/game/managers/obstacle_spawner.dart, lib/game/components/obstacle.dart, lib/game/config/game_config.dart</files>
  <action>
Make audio intensity scale with difficulty level, mirroring how visual danger tint subtly increases:

**Spawn cue volume scaling:**
- In ObstacleSpawner where spawn_cue is played, scale volume from 0.3 at difficulty 1 to 0.6 at difficulty 5.
- Formula: `0.3 + (difficultyManager.speedMultiplier - 1.0) * 0.5` clamped to 0.3-0.6.
- The spawn rhythm becomes more prominent as pressure increases.

**Near-miss volume scaling:**
- In Obstacle near-miss detection, scale near_miss volume from 0.5 to 1.0 based on difficulty.
- Formula: `0.5 + (game.difficultyManager.speedMultiplier - 1.0) * 0.83` clamped to 0.5-1.0.
- Near-misses feel more intense when the game is harder.

**Death impact always at full volume** — no scaling needed, it's already the punctuation mark.

Add config constants to GameConfig:
- `static const double spawnCueBaseVolume = 0.3;`
- `static const double spawnCueMaxVolume = 0.6;`
- `static const double nearMissBaseVolume = 0.5;`
- `static const double nearMissMaxVolume = 1.0;`

Keep dodge_whoosh at fixed volume — it's player-initiated feedback that should feel consistent regardless of difficulty.
  </action>
  <verify>Run game. At the start (difficulty 1), spawn cues should be quiet and near-misses moderate. Survive 60+ seconds to reach higher difficulty — spawn cues should be noticeably louder and near-misses more prominent. The change should feel gradual, not sudden.</verify>
  <done>Spawn cue and near-miss volume scale smoothly with difficulty. Dodge and death remain fixed. Audio intensity mirrors visual danger tint progression.</done>
</task>

<task type="auto">
  <name>Task 2: Sync spawn audio precisely with visual pulse</name>
  <files>lib/game/managers/obstacle_spawner.dart, lib/game/components/background_pulse.dart</files>
  <action>
Ensure spawn audio and visual pulse are perfectly synchronized and feel like one unified heartbeat:

**Verify sync in ObstacleSpawner:**
- The spawn_cue playSfx call and backgroundPulse.kick() call should be on consecutive lines in the same block (they should already be close from 05-02 wiring).
- If they're not adjacent, move them to be right next to each other so they fire on the exact same frame.
- The audio tick and visual pulse must feel like one event, not two.

**Add subtle bass layer on spawn:**
- Generate a new audio asset **pulse_bass.wav** (~0.15s) in the generate_audio.dart script: a very short, deep sine wave at 55Hz with fast attack and exponential decay. Amplitude ~0.3. This is a "felt more than heard" sub-bass thump.
- Create an AudioPool for pulse_bass in AudioManager.
- Play pulse_bass alongside spawn_cue in ObstacleSpawner at low volume (0.2 base, scaling to 0.4 at max difficulty).
- This bass layer gives the spawn rhythm physical weight that matches the visual background pulse.

Add to GameConfig:
- `static const double pulseBassBaseVolume = 0.2;`
- `static const double pulseBassMaxVolume = 0.4;`

Re-run `dart run tool/generate_audio.dart` to generate the new asset. Update AudioManager to preload it and create a pool.
  </action>
  <verify>Run game. Each obstacle spawn should produce a unified audio-visual pulse: visual background expansion + audio tick + subtle bass thump, all simultaneous. The bass should be felt more than heard. Verify at low and high difficulty — bass gets slightly louder at higher difficulty.</verify>
  <done>Spawn audio and visual pulse are frame-synchronized. Subtle bass layer adds physical weight to the rhythm. Volume scales with difficulty. New pulse_bass.wav generated and pooled.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Spawn cue volume increases noticeably from difficulty 1 to 5
- [ ] Near-miss volume increases with difficulty
- [ ] Audio and visual pulse are perfectly synchronized
- [ ] Bass layer is subtle but adds weight
- [ ] `C:/flutter/bin/flutter analyze` passes
- [ ] No audio clipping when multiple sounds play simultaneously
</verification>

<success_criteria>
- All tasks completed
- Audio intensity scales with difficulty (subtle at start, urgent at high levels)
- Spawn audio-visual sync feels like one unified heartbeat
- No new analyzer errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-audio-system/05-04-SUMMARY.md`
</output>
