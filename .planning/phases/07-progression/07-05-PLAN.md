---
phase: 07-progression
plan: 05
type: execute
---

<objective>
Create the collection/gallery screen for browsing and selecting unlocked shapes and themes.

Purpose: The gallery is where players engage with their progression — viewing what they've earned, previewing items, and selecting their loadout. This is the payoff screen that makes the XP grind feel worthwhile.
Output: Gallery overlay screen showing all shapes and themes with locked/unlocked/selected states, item selection, and navigation from main menu.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-progression/07-01-SUMMARY.md
@.planning/phases/07-progression/07-02-SUMMARY.md
@.planning/phases/07-progression/07-03-SUMMARY.md
@.planning/phases/07-progression/07-04-SUMMARY.md

# Key source files:
@lib/main.dart
@lib/screens/main_menu.dart
@lib/screens/game_over_screen.dart
@lib/game/pulse_game.dart
@lib/game/models/player_shape.dart
@lib/game/models/color_theme.dart
@lib/utils/progression_repository.dart
@lib/game/config/game_config.dart

**Tech stack available:** Flutter 3.41.0, Dart 3.11.0, flame ^1.35.0, shared_preferences ^2.3.0
**Established patterns:** Overlay-based screens via GameWidget overlayBuilderMap, StatefulWidget with game reference, existing screen styles (semi-transparent bg, white text, letterSpacing)
**Constraining decisions:**
- Phase 01-04: Overlay-driven UI, GameWidget.controlled with overlayBuilderMap
- Phase 01-02: GameWidget.controlled(gameFactory:) modern pattern
- Phase 07-02: PlayerShapes.all with id, name, xpCost, vertices
- Phase 07-03: ColorThemes.all with id, name, xpCost, color fields
- Phase 07-01: ProgressionRepository.isUnlocked(xpCost), selectedShapeId, selectedThemeId
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gallery screen with shapes and themes display</name>
  <files>lib/screens/gallery_screen.dart, lib/main.dart</files>
  <action>
Create GalleryScreen as a StatefulWidget in lib/screens/gallery_screen.dart:
- Takes PulseGame game reference (same pattern as all other screens)
- Semi-transparent dark background (matching game over screen style: Color(0xCC000000))
- Layout (scrollable Column):
  - "COLLECTION" title at top (same style as "GAME OVER" — white, bold, letterSpacing: 8, fontSize: 32)
  - Total XP display: "{totalXP} XP" in xpDisplayColor
  - "SHAPES" section header (smaller, alpha 0.6, letterSpacing: 4)
  - Horizontal row of shape items (Wrap or Row with wrapping)
  - "THEMES" section header
  - Horizontal row of theme items

Each shape item:
- A small preview of the shape (draw the shape's path using CustomPainter at ~30×30 scale)
- Shape name below
- If locked: grayed out with lock icon (🔒) and XP cost shown
- If unlocked but not selected: normal color, tappable
- If selected: highlighted border or glow effect, "EQUIPPED" label

Each theme item:
- A small color swatch showing the theme's playerColor on the theme's backgroundColor (simple circle or square)
- Theme name below
- Same locked/unlocked/selected states as shapes

Back button at bottom: "BACK" text button returning to main menu

Register 'Gallery' overlay in main.dart overlayBuilderMap:
```dart
'Gallery': (context, game) => GalleryScreen(game: game),
```
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub shows no errors</verify>
  <done>Gallery screen displays all shapes and themes with correct locked/unlocked/selected states. Registered as overlay.</done>
</task>

<task type="auto">
  <name>Task 2: Item selection interaction + main menu navigation</name>
  <files>lib/screens/gallery_screen.dart, lib/screens/main_menu.dart, lib/game/pulse_game.dart, lib/utils/audio_manager.dart</files>
  <action>
Selection interaction in GalleryScreen:
- Tapping an unlocked shape: call game.applyShape(shapeId), play menu_select.wav, setState to update selected indicator
- Tapping an unlocked theme: call game.applyTheme(themeId), play menu_select.wav, setState to update selected indicator AND refresh the gallery's own display colors (since the theme just changed)
- Tapping a locked item: brief visual feedback (e.g., slight shake or flash) but no action. Optionally show a tooltip-like text "{xpCost} XP needed" briefly.

Main menu integration:
- Add a "COLLECTION" button to MainMenu below "TAP TO PLAY":
  - Style: smaller text, alpha 0.5, letterSpacing: 4 (secondary action, doesn't compete with play)
  - Wrap in GestureDetector that calls game.showGallery()
  - The button should NOT be inside the full-screen GestureDetector that starts the game — it needs its own tap target

PulseGame navigation methods:
- showGallery(): removes 'MainMenu' overlay, adds 'Gallery' overlay
- hideGallery(): removes 'Gallery' overlay, adds 'MainMenu' overlay
- These are called from the gallery back button and main menu collection button respectively

Play menu_select.wav on navigation transitions (opening/closing gallery).
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub shows no errors. Run C:/flutter/bin/flutter run -d chrome. From main menu, tap COLLECTION to open gallery. Verify shapes and themes display correctly. Select an unlocked item, verify it applies. Tap BACK to return to menu. Start game, verify selected shape and theme are active.</verify>
  <done>Gallery screen fully interactive: item selection applies shape/theme immediately, persists across sessions. Main menu has COLLECTION button. Navigation works bidirectionally. Phase 7 complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] Gallery screen accessible from main menu
- [ ] All 5 shapes and 5 themes displayed with correct states
- [ ] Selecting an unlocked shape changes player shape
- [ ] Selecting an unlocked theme changes all game colors
- [ ] Locked items show XP cost and cannot be selected
- [ ] Selected items show visual indicator (equipped/active)
- [ ] Back button returns to main menu
- [ ] Selections persist across app restarts
- [ ] Game plays correctly with non-default shape and theme selected
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors introduced
- Gallery navigation works correctly (menu → gallery → menu → game)
- Shape and theme selection persists
- Game renders correctly with any shape/theme combination
- Phase 7: Progression & Unlockables complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-progression/07-05-SUMMARY.md`

Final plan success criteria: Phase 7 complete — all 5 plans executed, progression system functional with XP, unlockable shapes, unlockable themes, unlock notifications, and gallery screen.
</output>
