---
phase: 07-progression
plan: 04
type: execute
---

<objective>
Create the unlock condition system and reward triggers for newly unlocked items.

Purpose: The moment of unlocking a new item is the emotional peak of the progression system. This plan creates the logic that detects when XP crosses an unlock threshold and provides satisfying feedback when it happens.
Output: Unlock checking at game end, newly-unlocked detection, unlock notification with visual + audio feedback.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-progression/07-01-SUMMARY.md
@.planning/phases/07-progression/07-02-SUMMARY.md
@.planning/phases/07-progression/07-03-SUMMARY.md

# Key source files:
@lib/utils/progression_repository.dart
@lib/game/models/player_shape.dart
@lib/game/models/color_theme.dart
@lib/game/pulse_game.dart
@lib/screens/game_over_screen.dart
@lib/game/config/game_config.dart
@lib/utils/audio_manager.dart

**Tech stack available:** Flutter 3.41.0, Dart 3.11.0, flame ^1.35.0, shared_preferences ^2.3.0
**Established patterns:** ScoreRepository.recordGameEnd() returns isNewBest, high score fanfare audio on new best, AnimationController pulsing text on game over
**Constraining decisions:**
- Phase 06-04: isNewBest check BEFORE saveBestScore to avoid race condition
- Phase 06-04: Rising arpeggio for celebration audio
- Phase 07-01: ProgressionRepository.isUnlocked(xpCost) checks totalXP >= xpCost
- Phase 07-02: PlayerShapes.all list with xpCost per shape
- Phase 07-03: ColorThemes.all list with xpCost per theme
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detect newly unlocked items at game end</name>
  <files>lib/utils/progression_repository.dart, lib/game/pulse_game.dart, lib/game/config/game_config.dart</files>
  <action>
Add unlock detection to ProgressionRepository:
- Add a method `checkNewUnlocks(int xpBefore, int xpAfter)` that returns a list of newly unlocked items:
  - Iterate through all PlayerShapes.all and ColorThemes.all
  - An item is "newly unlocked" if xpBefore < item.xpCost <= xpAfter
  - Return a List of unlock info objects (or simple records/tuples with name and type: 'shape'/'theme')
- Create a simple UnlockInfo class: name (String), type (String — 'shape' or 'theme'), id (String)

In PulseGame.gameOver():
- Before calling addXP(), capture xpBefore = ProgressionRepository.instance.totalXP
- After addXP(), capture xpAfter = ProgressionRepository.instance.totalXP
- Call checkNewUnlocks(xpBefore, xpAfter) and store result as `List<UnlockInfo> lastNewUnlocks` field on PulseGame
- This list is read by the game over screen to display unlock notifications

Add to GameConfig:
- unlockChimeVolume: 0.7 (volume for unlock sound)
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub shows no errors</verify>
  <done>checkNewUnlocks() correctly identifies items whose XP threshold was crossed during this game. lastNewUnlocks stored on PulseGame for game over screen access.</done>
</task>

<task type="auto">
  <name>Task 2: Unlock notification on game over screen</name>
  <files>lib/screens/game_over_screen.dart, lib/utils/audio_manager.dart, tool/generate_audio.dart</files>
  <action>
Generate an unlock chime sound:
- In tool/generate_audio.dart, add a new "unlock_chime.wav" generation — a bright ascending two-note chime (different from high_score_fanfare which is a 3-note arpeggio). Think: a quick "ding-ding!" notification sound. Two notes: E5 then G5, each ~0.1s, with a quick sine envelope.
- Register in AudioManager for preloading (not pooled — low frequency event)

In GameOverScreen:
- If game.lastNewUnlocks is not empty, display unlock notifications between the XP display and "TAP TO RETRY":
  - For each unlock: "🔓 UNLOCKED: {name}" in a bright accent color (use GameConfig.xpDisplayColor or similar teal/cyan)
  - Use a subtle entrance animation (fade in with slight scale) via AnimationController — similar to the existing _pulseController pattern but with a delayed start
  - Play unlock chime sound when the widget first builds with unlocks (in initState, after a short delay to not overlap with death sound)

Keep it simple — no complex animations. Just clear, satisfying text notifications that the player can read before tapping to retry.

If multiple items unlock simultaneously, show each on its own line. Cap display at 3 items max (very unlikely to unlock more, but prevents overflow).
  </action>
  <verify>C:/flutter/bin/flutter analyze --no-pub shows no errors. Generate the audio: cd C:/claude/ClaudesChoice && C:/flutter/bin/dart run tool/generate_audio.dart. Run C:/flutter/bin/flutter run -d chrome, set totalXP artificially low (0) in shared_preferences or temporarily hardcode, play until score crosses a threshold, verify unlock notification appears.</verify>
  <done>Game over screen shows "UNLOCKED: {name}" for newly unlocked items with chime audio. Multiple unlocks shown on separate lines.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] unlock_chime.wav generated and plays correctly
- [ ] Newly unlocked items detected correctly when XP crosses threshold
- [ ] Unlock notification displays on game over screen
- [ ] No notification shown when no new unlocks occur
- [ ] Multiple simultaneous unlocks display correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors introduced
- Unlock detection is correct (only triggers on threshold crossing, not retroactively)
- Unlock notification is visible and readable on game over screen
- Unlock chime audio plays at appropriate volume
</success_criteria>

<output>
After completion, create `.planning/phases/07-progression/07-04-SUMMARY.md`
</output>
