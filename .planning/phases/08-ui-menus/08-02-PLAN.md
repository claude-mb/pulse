---
phase: 08-ui-menus
plan: 02
type: execute
---

<objective>
Build the full settings screen with audio controls, audio mute persistence, and navigation to gallery.

Purpose: Give players a dedicated place to control audio and access customization, with mute preferences persisted across app restarts.
Output: Fully functional SettingsScreen with mute toggles, gallery link, and SharedPreferences persistence for audio state.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ui-menus/08-01-SUMMARY.md

@lib/screens/settings_screen.dart
@lib/screens/pause_overlay.dart
@lib/utils/audio_manager.dart
@lib/game/pulse_game.dart
@lib/game/config/game_config.dart
@lib/utils/progression_repository.dart

**Tech stack available:** Flutter 3.41.0, shared_preferences ^2.3.0
**Established patterns:**
- Audio toggles: icon + label, muted at 0.5 opacity (pause_overlay.dart)
- ProgressionRepository uses SharedPreferences singleton pattern
- GestureDetector + Text for buttons (not TextButton)
- GameConfig.textColor, xpDisplayColor for consistent theming
**Constraining decisions:**
- Mute toggles only, no volume sliders for v1 (05-05)
- Track last-requested BGM for unmute resume (05-05)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build settings screen with audio toggles and gallery link</name>
  <files>lib/screens/settings_screen.dart</files>
  <action>Replace the placeholder SettingsScreen (created in 08-01) with a full StatefulWidget. Layout:

1. "SETTINGS" title (GameConfig.textColor, 32px, bold, letterSpacing 8) — centered at top
2. "AUDIO" section header (textColor at 0.6 alpha, 14px, letterSpacing 4)
3. SFX toggle row: icon (volume_up/volume_off) + "SFX" label + on/off state text. Use AudioManager.instance for state. Tapping toggles via audio.toggleSfxMute() and setState(). Muted state shown at 0.5 opacity (matching pause_overlay pattern).
4. Music toggle row: icon (music_note/music_off) + "MUSIC" label + on/off state. Same pattern.
5. Thin divider (200px wide, textColor at 0.2 alpha)
6. "CUSTOMIZATION" section header
7. Currently equipped shape name + theme name displayed as read-only text (from ProgressionRepository.instance.selectedShapeId / selectedThemeId, resolved to display names via PlayerShapes.getById / ColorThemes.getById)
8. "VIEW COLLECTION" button — navigates to gallery. In PulseGame, add showGalleryFromSettings() that removes 'Settings' and adds 'Gallery', and modify hideGallery() to check what screen to return to (add a _returnFromGalleryTo field that tracks whether gallery was opened from main menu or settings).
9. "BACK" button at bottom — calls game.hideSettings()

Use SafeArea, Center, SingleChildScrollView for layout (matching GalleryScreen pattern). All colors via GameConfig. Container color: Color(0xCC000000) matching other overlay screens.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>Settings screen shows audio toggles, equipped shape/theme names, VIEW COLLECTION link, and BACK button. Audio toggles work. Gallery navigation from settings returns to settings (not main menu).</done>
</task>

<task type="auto">
  <name>Task 2: Add audio mute state persistence</name>
  <files>lib/utils/audio_manager.dart</files>
  <action>Add SharedPreferences persistence for sfxMuted and bgmMuted states. In initialize(), after setting _initialized = true, load saved mute states from SharedPreferences using keys 'audio_sfx_muted' and 'audio_bgm_muted' (bool, default false). Apply loaded states to sfxMuted and bgmMuted fields.

In toggleSfxMute() and toggleBgmMute(), after toggling the boolean, save the new state to SharedPreferences asynchronously (fire-and-forget, no await needed — same pattern as ProgressionRepository). Import shared_preferences package.

Do NOT add volume persistence (sliders are deferred per 05-05 decision). Only persist mute toggles.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>Mute states saved to SharedPreferences on toggle, loaded on initialize(). Mute preferences persist across app restarts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] Settings screen displays all sections (audio toggles, customization, navigation)
- [ ] SFX/Music mute toggles work and update visual state
- [ ] VIEW COLLECTION navigates to gallery and BACK from gallery returns to settings
- [ ] BACK from settings returns to main menu
- [ ] Audio mute states persist (toggle mute, would survive restart)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors introduced
- Settings screen fully functional with audio controls and gallery link
- Audio mute persistence implemented
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-menus/08-02-SUMMARY.md`
</output>
