---
phase: 08-ui-menus
plan: 03
type: execute
---

<objective>
Add animated transitions to all overlay screens for polished screen changes instead of instant swap.

Purpose: Smooth transitions between screens create a cohesive, polished feel that matches the game's visual quality. Instant overlay swaps feel jarring.
Output: All major overlay screens fade in smoothly. Game state transitions feel connected.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ui-menus/08-01-SUMMARY.md
@.planning/phases/08-ui-menus/08-02-SUMMARY.md

@lib/screens/main_menu.dart
@lib/screens/game_over_screen.dart
@lib/screens/settings_screen.dart
@lib/screens/gallery_screen.dart
@lib/screens/pause_overlay.dart
@lib/game/pulse_game.dart
@lib/main.dart

**Tech stack available:** Flutter 3.41.0, Flame ^1.35.0
**Established patterns:**
- GameOverScreen already uses AnimationController + TickerProviderStateMixin for pulse animation
- FadeTransition used for unlock notifications in game_over_screen.dart
- Overlays managed via game.overlays.add/remove
**Constraining decisions:**
- Flame overlays are Flutter widgets rendered on top of GameWidget — standard Flutter animation works
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fade-in animations to overlay screens</name>
  <files>lib/screens/main_menu.dart, lib/screens/game_over_screen.dart, lib/screens/settings_screen.dart, lib/screens/gallery_screen.dart, lib/screens/pause_overlay.dart</files>
  <action>Add a fade-in animation to each overlay screen that plays when the screen appears. Pattern for each screen:

1. Ensure the widget is a StatefulWidget with TickerProviderStateMixin (MainMenu was converted in 08-01; GameOverScreen and PauseOverlay already are StatefulWidgets — add TickerProviderStateMixin if not present; convert GalleryScreen and SettingsScreen if needed).
2. Add an AnimationController _fadeController with duration 200ms (fast, snappy — not sluggish).
3. In initState(), call _fadeController.forward() to start the fade.
4. Wrap the top-level Container in a FadeTransition(opacity: _fadeController, child: ...).
5. Dispose the controller in dispose().

Use 200ms for most screens. Use 300ms for GameOverScreen (slightly slower for dramatic effect after death). Do NOT animate HudOverlay — it should appear instantly during gameplay.

For MainMenu specifically, skip the fade if the app just launched (it's the initial overlay). Only fade when returning to menu from other screens. To handle this: add an optional `animate` parameter to MainMenu's constructor, defaulting to true. In the overlayBuilderMap in main.dart, always pass animate: true (Flame rebuilds the overlay each time it's added). The initial overlay on first load will also animate, which is fine — a subtle 200ms fade on app start looks polished, not slow.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>All overlay screens (MainMenu, GameOver, Settings, Gallery, Pause) fade in over 200-300ms when shown. HudOverlay unchanged. No analyzer errors.</done>
</task>

<task type="auto">
  <name>Task 2: Smooth game-start transition with HUD delay</name>
  <files>lib/game/pulse_game.dart</files>
  <action>When starting a game (startGame()), the MainMenu overlay is removed and HUD is added. To make this smoother, add a very brief delay (100ms) before adding the HUD overlay using Future.delayed. This creates a clean visual beat between the menu disappearing and HUD appearing.

When the game ends (gameOver()), the transition is already dramatic (slow-mo + death effects + flash), so just ensure GameOver overlay add timing feels natural — the existing delayed pause already handles this.

When returning to menu (returnToMenu()), ensure all game overlays are removed before adding MainMenu. The current code already does this correctly.

This is a subtle timing polish — do NOT introduce any blocking or complex state management. Simple Future.delayed for the HUD appearance is sufficient.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>Game start has subtle visual beat between menu removal and HUD appearance. Transition timing feels smooth and intentional.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] MainMenu fades in when navigating back from game over or settings
- [ ] GameOverScreen fades in (300ms) after death
- [ ] Settings and Gallery screens fade in when opened
- [ ] Pause overlay fades in when pausing
- [ ] HUD appears after brief delay when game starts
- [ ] No visual glitches during transitions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors introduced
- All overlay transitions feel smooth and polished
- No jarring instant-swap screen changes remain
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-menus/08-03-SUMMARY.md`
</output>
