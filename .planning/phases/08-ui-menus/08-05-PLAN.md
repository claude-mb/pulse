---
phase: 08-ui-menus
plan: 05
type: execute
---

<objective>
Standardize UI styling across all overlay screens and verify complete navigation flow.

Purpose: Ensure visual consistency and a complete, bug-free navigation experience across every screen and game state.
Output: All screens use consistent styling patterns. All navigation paths verified working.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ui-menus/08-04-SUMMARY.md

@lib/screens/main_menu.dart
@lib/screens/game_over_screen.dart
@lib/screens/settings_screen.dart
@lib/screens/gallery_screen.dart
@lib/screens/pause_overlay.dart
@lib/screens/tutorial_overlay.dart
@lib/screens/hud_overlay.dart
@lib/game/pulse_game.dart
@lib/main.dart

**Tech stack available:** Flutter 3.41.0, Flame ^1.35.0
**Established patterns:**
- GameConfig.textColor for all text (white)
- GestureDetector + HitTestBehavior.opaque for buttons
- Container color: Color(0xCC000000) for overlay backgrounds
- letterSpacing 4-8 for labels
**Constraining decisions:**
- Mute toggles only, no volume sliders (05-05)
- Minimal, geometric aesthetic (PROJECT.md)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize PauseOverlay styling to match other screens</name>
  <files>lib/screens/pause_overlay.dart</files>
  <action>The PauseOverlay currently uses hardcoded Colors.white, Colors.white70, Colors.white54, and TextButton widgets, which is inconsistent with other overlays that use GameConfig.textColor and GestureDetector.

Update PauseOverlay:
1. Replace Container color Colors.black54 with Color(0xCC000000) to match other overlays.
2. Replace "PAUSED" text color with GameConfig.textColor (currently Colors.white).
3. Replace RESUME TextButton with GestureDetector + Text pattern: GestureDetector(onTap: ..., behavior: HitTestBehavior.opaque, child: Padding(padding: EdgeInsets.all(12), child: Text('RESUME', style: TextStyle(color: GameConfig.textColor.withValues(alpha: 0.7), fontSize: 24, letterSpacing: 4)))).
4. Replace QUIT TextButton same way: GameConfig.textColor.withValues(alpha: 0.5), fontSize: 20.
5. Update audio toggle icons/labels to use GameConfig.textColor instead of Colors.white.
6. Import game_config.dart.

Keep all functionality identical — this is purely a styling consistency pass.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>PauseOverlay uses GameConfig.textColor, GestureDetector buttons, and Color(0xCC000000) background — consistent with all other overlays.</done>
</task>

<task type="auto">
  <name>Task 2: Add settings access from pause overlay</name>
  <files>lib/screens/pause_overlay.dart, lib/game/pulse_game.dart</files>
  <action>Add a SETTINGS button to the PauseOverlay below the audio toggles, allowing players to access settings during gameplay pause.

In PauseOverlay, add a SizedBox(height: 24) after the audio toggle Row, then a GestureDetector SETTINGS button (same style as other nav buttons: textColor at 0.5 alpha, 16px, letterSpacing 4). On tap, call game.showSettingsFromPause().

In PulseGame, add showSettingsFromPause() that removes 'Pause' overlay and adds 'Settings'. Update the _returnFromSettings tracking (similar to gallery return-to tracking) so hideSettings() knows to return to 'Pause' (not 'MainMenu') when settings was opened from pause.

This means the _returnFrom tracking pattern needs to work for both gallery and settings. Use a simple String? _previousScreen field that tracks where we came from. Set it in showSettings(), showSettingsFromPause(), showGalleryFromSettings(). Read it in hideSettings(), hideGallery() to determine which overlay to restore.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>SETTINGS accessible from pause overlay. Settings → Back returns to pause (not main menu). Navigation state correctly tracks where settings/gallery was opened from.</done>
</task>

<task type="auto">
  <name>Task 3: Verify and fix all navigation flows</name>
  <files>lib/game/pulse_game.dart</files>
  <action>Audit every navigation path in PulseGame and ensure correctness. The complete navigation map should be:

1. App launch → MainMenu (initial overlay)
2. MainMenu → tap to play → Tutorial (first time) or gameplay (HUD)
3. Tutorial → tap → gameplay (HUD)
4. Gameplay → pause button → Pause
5. Pause → RESUME → gameplay
6. Pause → QUIT → MainMenu
7. Pause → SETTINGS → Settings → BACK → Pause
8. Settings (from pause) → VIEW COLLECTION → Gallery → BACK → Settings → BACK → Pause
9. MainMenu → SETTINGS → Settings → BACK → MainMenu
10. Settings (from menu) → VIEW COLLECTION → Gallery → BACK → Settings → BACK → MainMenu
11. MainMenu → COLLECTION → Gallery → BACK → MainMenu
12. Gameplay → death → GameOver
13. GameOver → tap → gameplay (HUD)
14. GameOver → MENU → MainMenu

For each path, verify the overlay add/remove sequence is correct. Ensure no stale overlays remain (e.g., if returning to menu from pause, both Pause and HUD are removed). Ensure the game.paused flag is correctly set for each state (paused in menu/pause/gameOver/settings/gallery, unpaused only during gameplay).

Fix any edge cases found. Pay special attention to:
- Gallery opened from MainMenu vs Settings (return target differs)
- Settings opened from MainMenu vs Pause (return target differs)
- Audio state (BGM should stop when returning to menu, stay paused when going to settings from pause)</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>All 14 navigation paths verified correct. No stale overlays. Pause/unpause state correct in all transitions. Return-to tracking works for all settings/gallery entry points.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] PauseOverlay uses consistent styling (GameConfig colors, GestureDetector buttons)
- [ ] Settings accessible from both main menu and pause overlay
- [ ] Gallery accessible from both main menu and settings
- [ ] BACK buttons return to correct previous screen in all cases
- [ ] No stale overlays remain after any navigation sequence
- [ ] Game pause state correct in all screens (only unpaused during gameplay)
- [ ] Phase 8 complete — all UI & Menu functionality implemented
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors introduced
- All screens visually consistent
- Complete navigation flow with no dead ends or edge cases
- Phase 8: UI & Menus complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-menus/08-05-SUMMARY.md`
</output>
