---
phase: 08-ui-menus
plan: 04
type: execute
---

<objective>
Add a minimal first-time tutorial overlay that teaches new players the tap-to-dodge mechanic.

Purpose: New players need to understand the core mechanic before dying immediately. A one-time instruction on first game prevents confusion without annoying returning players.
Output: TutorialOverlay widget shown once on first-ever game start, with "has seen tutorial" persistence.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ui-menus/08-03-SUMMARY.md

@lib/game/pulse_game.dart
@lib/main.dart
@lib/game/config/game_config.dart

**Tech stack available:** Flutter 3.41.0, shared_preferences ^2.3.0
**Established patterns:**
- SharedPreferences for persistence (ScoreRepository, ProgressionRepository, AudioManager)
- Overlays registered in main.dart overlayBuilderMap
- Game paused/unpaused via game.paused = true/false
- GestureDetector + HitTestBehavior.opaque for tap handling
**Constraining decisions:**
- Minimal tutorial — show tap instruction, not lengthy onboarding (roadmap spec)
- Game starts paused (paused = true in onLoad)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tutorial overlay widget</name>
  <files>lib/screens/tutorial_overlay.dart, lib/main.dart</files>
  <action>Create a new TutorialOverlay StatefulWidget in lib/screens/tutorial_overlay.dart. Layout:

1. Semi-transparent dark background (Color(0xCC000000) — matching other overlays)
2. Center column with:
   - "HOW TO PLAY" title (GameConfig.textColor, 28px, bold, letterSpacing 6)
   - SizedBox(height: 32)
   - Left arrow icon (Icons.chevron_left, white, 40px) + "TAP LEFT" text + "TO DODGE LEFT" subtitle
   - SizedBox(height: 24)
   - Right arrow icon (Icons.chevron_right, white, 40px) + "TAP RIGHT" text + "TO DODGE RIGHT" subtitle
   - SizedBox(height: 16)
   - "DODGE OBSTACLES TO SURVIVE" (textColor at 0.5 alpha, 14px)
   - SizedBox(height: 40)
   - "TAP TO START" (textColor at 0.7 alpha, 20px, letterSpacing 4)

Wrap in GestureDetector with onTap that calls game.dismissTutorial(). Use FadeTransition with 300ms fade-in on initState.

The left/right instruction should use a Row with Icon + Column(label, subtitle) for each direction. Keep it simple and geometric — no images, just icons and text.

Register 'Tutorial' in main.dart overlayBuilderMap: (context, game) => TutorialOverlay(game: game).</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>TutorialOverlay widget created with clear tap-to-dodge instructions. Registered in overlayBuilderMap. Fades in on display.</done>
</task>

<task type="auto">
  <name>Task 2: Add tutorial trigger and persistence logic</name>
  <files>lib/game/pulse_game.dart</files>
  <action>Add tutorial state management to PulseGame:

1. Add a bool _hasSeenTutorial field, initialized to false.
2. In onLoad(), after initializing repositories, load the tutorial flag from SharedPreferences: _hasSeenTutorial = prefs.getBool('has_seen_tutorial') ?? false. Get the SharedPreferences instance (import shared_preferences).
3. Modify startGame(): if !_hasSeenTutorial, instead of starting gameplay, show the tutorial overlay:
   - Remove 'MainMenu' overlay
   - Add 'Tutorial' overlay
   - Do NOT unpause or start gameplay yet
   - Return early
4. Add dismissTutorial() method:
   - Set _hasSeenTutorial = true
   - Save to SharedPreferences (fire-and-forget): SharedPreferences.getInstance().then((p) => p.setBool('has_seen_tutorial', true))
   - Remove 'Tutorial' overlay
   - Call the real game start sequence (set state to playing, unpause, add HUD, play SFX/BGM, reset everything — extract the body of startGame() into a _beginGameplay() private method, call it from both startGame() (when tutorial already seen) and dismissTutorial())

This ensures the tutorial shows exactly once on the very first game start, then never again.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>Tutorial shows on first-ever game start. Tapping dismisses and starts gameplay. Flag persisted — subsequent starts skip tutorial. Second+ game starts go directly to gameplay.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] On fresh state (clear SharedPreferences or first launch), tapping "TAP TO PLAY" shows tutorial
- [ ] Tutorial displays clear left/right dodge instructions
- [ ] Tapping tutorial dismisses it and starts gameplay
- [ ] Subsequent game starts skip tutorial and go directly to gameplay
- [ ] Tutorial flag persists across app restarts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors introduced
- First-time players see clear instructions
- Returning players experience zero friction
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-menus/08-04-SUMMARY.md`
</output>
