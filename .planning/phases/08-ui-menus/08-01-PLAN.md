---
phase: 08-ui-menus
plan: 01
type: execute
---

<objective>
Enhance the main menu with animated branding, high score display, and settings navigation entry point.

Purpose: Transform the bare-bones main menu into a polished title screen that conveys the game's identity and provides clear navigation options.
Output: Visually enhanced MainMenu with pulsing title, best score, and settings/collection buttons.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-progression/07-05-SUMMARY.md

@lib/screens/main_menu.dart
@lib/game/pulse_game.dart
@lib/main.dart
@lib/game/config/game_config.dart
@lib/utils/score_repository.dart

**Tech stack available:** Flutter 3.41.0, Flame ^1.35.0, shared_preferences ^2.3.0
**Established patterns:**
- Flame overlay widgets receive PulseGame game reference
- GestureDetector + opaque hit behavior for buttons (not TextButton)
- GameConfig for all color references (textColor, xpDisplayColor, etc.)
- overlayBuilderMap in main.dart registers all overlays
- ScoreRepository.instance.bestScore for high scores
**Constraining decisions:**
- Stack layout in MainMenu for separate tap targets (07-05)
- GameConfig color getters delegate to activeTheme (07-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add animated pulsing title and high score display</name>
  <files>lib/screens/main_menu.dart</files>
  <action>Convert MainMenu to a StatefulWidget with TickerProviderStateMixin. Add an AnimationController that drives a subtle scale pulse (1.0 to 1.05, ~2s period, repeat reverse) on the "PULSE" title text using ScaleTransition. Add a soft glow effect using a second Text widget behind the title with slightly larger font and low opacity (0.15) that pulses in sync. Below the title/subtitle, add a high score display: "BEST [score]" using ScoreRepository.instance.bestScore, styled like the HUD best-score (GameConfig.textColor at 0.5 alpha, 16px, letterSpacing 2). Only show the best score row if bestScore > 0.

Use GameConfig.textColor for all text colors. Use GameConfig.accentColor for the glow text behind the title. Keep the GestureDetector full-screen tap-to-play pattern intact. Do NOT use TextButton — maintain the GestureDetector + Text pattern used throughout the project.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>MainMenu is StatefulWidget with pulsing title animation, glow effect, and conditional best score display below subtitle</done>
</task>

<task type="auto">
  <name>Task 2: Add settings button and reorganize navigation layout</name>
  <files>lib/screens/main_menu.dart, lib/game/pulse_game.dart, lib/main.dart</files>
  <action>In the MainMenu's bottom area, reorganize the layout: place SETTINGS and COLLECTION buttons side-by-side in a Row (centered, with 32px spacing between them). Both use the same style: GameConfig.textColor at 0.5 alpha, 16px, letterSpacing 4, each wrapped in GestureDetector with HitTestBehavior.opaque and 12px padding.

In PulseGame, add a showSettings() method that plays menu_select.wav SFX, removes 'MainMenu' overlay, and adds 'Settings' overlay. Add a hideSettings() method that plays menu_select.wav, removes 'Settings', and adds 'MainMenu'. These follow the exact same pattern as showGallery()/hideGallery().

In main.dart, register the 'Settings' overlay in overlayBuilderMap. Import the settings screen (which will be created in plan 08-02). For now, create a minimal placeholder SettingsScreen widget in lib/screens/settings_screen.dart — just a Container with "SETTINGS" text and a BACK button that calls game.hideSettings(). This placeholder will be fully built out in plan 08-02.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>Main menu has side-by-side SETTINGS and COLLECTION buttons at bottom. PulseGame has showSettings()/hideSettings(). Settings overlay registered in main.dart with placeholder widget. Navigation works: main menu → settings → main menu.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] MainMenu shows animated pulsing title
- [ ] High score displays when bestScore > 0
- [ ] SETTINGS and COLLECTION buttons visible at bottom of main menu
- [ ] Tapping SETTINGS navigates to settings overlay
- [ ] Settings BACK button returns to main menu
- [ ] Tapping anywhere else on main menu still starts the game
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors or warnings introduced
- Main menu has polished branding with animated title
- Navigation to settings screen works bidirectionally
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-menus/08-01-SUMMARY.md`
</output>
