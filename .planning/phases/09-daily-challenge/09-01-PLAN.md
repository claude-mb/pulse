---
phase: 09-daily-challenge
plan: 01
type: execute
---

<objective>
Create the seeded random number generator infrastructure and game mode enum for daily challenges.

Purpose: Establish the deterministic RNG foundation that makes daily challenges produce the same obstacle sequence for all players on a given day.
Output: Date-based seed utility, GameMode enum, and ObstacleSpawner modified to accept optional seed.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ui-menus/08-05-SUMMARY.md

@lib/game/pulse_game.dart
@lib/game/managers/obstacle_spawner.dart
@lib/game/managers/pattern_sequencer.dart
@lib/game/config/game_config.dart

**Tech stack available:** Flutter 3.41.0, Flame ^1.35.0, dart:math Random, shared_preferences ^2.3.0
**Established patterns:**
- ObstacleSpawner creates Random() and passes to PatternSequencer via constructor
- PatternSequencer uses the shared Random for all pattern selection and generation
- ObstacleSpawner also uses _random for width fallback (line 67-70)
- Components are Flame Component subclasses with HasGameReference<PulseGame>
- GameConfig holds all static constants
- Enums defined at top of pulse_game.dart (GameState)
**Constraining decisions:**
- All pattern randomness flows through single Random instance in ObstacleSpawner (02-05)
- PatternSequencer receives Random via constructor, no internal Random creation (03-01)
- DifficultyManager is purely time-based, not random — deterministic already (03-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create date-based seed utility and GameMode enum</name>
  <files>lib/utils/daily_seed.dart, lib/game/pulse_game.dart</files>
  <action>Create lib/utils/daily_seed.dart with two functions:
- `int dailySeedForDate(DateTime date)` — returns `date.year * 10000 + date.month * 100 + date.day` (e.g., 20260214 for Feb 14, 2026). This produces a unique integer per calendar day, suitable as a Random seed.
- `int todaysSeed()` — returns `dailySeedForDate(DateTime.now())`. Convenience wrapper.

In lib/game/pulse_game.dart, add a GameMode enum BEFORE the existing GameState enum:
```dart
enum GameMode { endless, daily }
```

Add two fields to PulseGame:
- `GameMode _gameMode = GameMode.endless;`
- `GameMode get gameMode => _gameMode;`

Do NOT modify any existing methods yet — that's Plan 09-02/09-03.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>daily_seed.dart exists with dailySeedForDate and todaysSeed functions. GameMode enum and field exist in PulseGame. Analyzer passes.</done>
</task>

<task type="auto">
  <name>Task 2: Modify ObstacleSpawner to accept optional seed</name>
  <files>lib/game/managers/obstacle_spawner.dart</files>
  <action>Modify ObstacleSpawner to support seeded RNG:

1. Change field declarations from:
   ```dart
   final Random _random = Random();
   late final PatternSequencer _sequencer = PatternSequencer(random: _random);
   ```
   to non-final fields with late initialization:
   ```dart
   late Random _random;
   late PatternSequencer _sequencer;
   ```

2. Add a constructor that accepts optional seed:
   ```dart
   ObstacleSpawner({int? seed}) {
     _random = seed != null ? Random(seed) : Random();
     _sequencer = PatternSequencer(random: _random);
   }
   ```

3. Modify the reset() method to accept optional seed:
   ```dart
   void reset({int? seed}) {
     _elapsed = 0;
     if (seed != null) {
       _random = Random(seed);
       _sequencer = PatternSequencer(random: _random);
     } else {
       _sequencer.reset();
     }
   }
   ```

   This way: `reset()` (no args) preserves endless behavior — clears sequencer state, Random continues its sequence. `reset(seed: X)` creates a fresh seeded Random + new PatternSequencer, producing deterministic patterns from the seed.

The rest of update() is unchanged — it reads from _random and _sequencer as before. PatternSequencer already receives Random via constructor, so no changes needed there.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors. Grep for "ObstacleSpawner(" in the codebase to verify all call sites still compile (pulse_game.dart line ~140 creates it without args, which still works).</verify>
  <done>ObstacleSpawner accepts optional seed in constructor and reset(). Existing callers (no-arg construction and reset()) work unchanged. Analyzer passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] lib/utils/daily_seed.dart exists with correct seed generation
- [ ] GameMode enum exists in pulse_game.dart
- [ ] ObstacleSpawner constructor accepts optional seed
- [ ] ObstacleSpawner.reset() accepts optional seed
- [ ] Existing ObstacleSpawner() call in pulse_game.dart still compiles
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors or warnings introduced
- Seed utility produces deterministic date-based integers
- ObstacleSpawner is backward-compatible (no-arg usage unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/09-daily-challenge/09-01-SUMMARY.md`
</output>
