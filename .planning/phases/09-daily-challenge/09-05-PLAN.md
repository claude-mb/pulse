---
phase: 09-daily-challenge
plan: 05
type: execute
---

<objective>
Create mode-aware game over screen for daily challenges with streak display and full flow verification.

Purpose: Give players meaningful feedback after a daily challenge run — daily best, attempts today, streak — and celebrate new daily bests. Verify the complete daily challenge flow works end-to-end.
Output: Mode-aware GameOverScreen with daily-specific stats and streak display. Verified complete daily challenge flow.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-daily-challenge/09-04-SUMMARY.md

@lib/screens/game_over_screen.dart
@lib/game/pulse_game.dart
@lib/game/config/game_config.dart
@lib/utils/daily_challenge_repository.dart
@lib/utils/score_repository.dart

**Tech stack available:** Flutter 3.41.0, Flame overlays
**Established patterns:**
- GameOverScreen: StatefulWidget with TickerProviderStateMixin, FadeTransition, full-screen GestureDetector for retry
- Score display: large score number, NEW BEST! with pulse animation, BEST X fallback
- Lifetime stats: Row of _lifetimeStat columns (label + value)
- XP display: teal color (GameConfig.xpDisplayColor), +X XP and Total line
- Bottom: MENU button Positioned at bottom center
- game.lastRunWasNewBest boolean drives NEW BEST! celebration
- AnimatedBuilder with opacity pulse for NEW BEST! text
**Constraining decisions:**
- GestureDetector + HitTestBehavior.opaque for all buttons (08-05)
- game.lastDailyWasNewBest drives daily celebration (09-03)
- game.gameMode determines which stats to show (09-01)
- Tap-to-retry calls resetGame() which preserves current game mode (09-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mode-aware GameOverScreen with daily stats and streak</name>
  <files>lib/screens/game_over_screen.dart</files>
  <action>Modify GameOverScreen to show different content based on game mode. Import DailyChallengeRepository and GameMode.

1. **Title**: When `game.gameMode == GameMode.daily`, show "DAILY CHALLENGE" (in GameConfig.accentColor, 32px, bold, letterSpacing 6) instead of "GAME OVER" (48px). This immediately signals the context.

2. **Score section**: Keep the SCORE label and score number identical for both modes.

3. **Best score comparison**: Branch on game mode:
   - **Endless mode** (existing behavior): Show "NEW BEST!" pulse animation when lastRunWasNewBest, else "BEST X" from ScoreRepository.
   - **Daily mode**: Show "NEW DAILY BEST!" (GameConfig.accentColor, bold) with the same pulse animation when game.lastDailyWasNewBest, else "DAILY BEST X" from DailyChallengeRepository.instance.dailyBestScore.

   Use the existing _pulseController and _pulseAnimation — just change which boolean drives `_pulseController.repeat(reverse: true)` in initState:
   ```dart
   final showBestPulse = game.gameMode == GameMode.daily
       ? game.lastDailyWasNewBest
       : game.lastRunWasNewBest;
   if (showBestPulse) {
     _pulseController.repeat(reverse: true);
   }
   ```

4. **Run stats row**: Keep identical for both modes (dodges, near-misses, best combo). Survival time too.

5. **Lifetime stats section**: Branch on game mode:
   - **Endless mode** (existing): Show GAMES, BEST TIME, TOTAL DODGES, BEST COMBO rows from ScoreRepository.
   - **Daily mode**: Replace with daily-specific stats using the same _lifetimeStat helper:
     - Row 1: "ATTEMPTS" showing DailyChallengeRepository.instance.dailyAttempts, and "STREAK" showing DailyChallengeRepository.instance.streak with " DAYS" suffix
     - Row 2: "DAILY BEST" showing DailyChallengeRepository.instance.dailyBestScore, and "GAMES" showing ScoreRepository.instance.gamesPlayed (lifetime context)

6. **XP section**: Keep identical — daily games earn XP the same way.

7. **Unlock section**: Keep identical — works the same.

8. **TAP TO RETRY**: Keep identical — resetGame() already handles mode-aware retry (same seed in daily mode from 09-03).

9. **MENU button**: Keep identical — returnToMenu() already resets mode (09-03).

The key principle: minimize changes. Only the title, best-score comparison, and lifetime stats section change. Everything else (score, run stats, XP, unlocks, retry, menu) works identically.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors</verify>
  <done>GameOverScreen shows "DAILY CHALLENGE" title in daily mode with daily-specific best/streak/attempts stats. Shows "GAME OVER" with lifetime stats in endless mode. Analyzer passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and complete daily challenge flow test</name>
  <files>(no files modified — verification only)</files>
  <action>Run full build and analyze to verify the complete Phase 9 implementation:

1. Run `C:/flutter/bin/flutter analyze --no-pub` — must pass with zero errors.
2. Run `C:/flutter/bin/flutter build web --release --no-pub` — must complete successfully.
3. Verify all new files exist:
   - lib/utils/daily_seed.dart
   - lib/utils/daily_challenge_repository.dart
4. Verify all modified files have no issues:
   - lib/game/pulse_game.dart (GameMode, startDailyChallenge, mode-aware methods)
   - lib/game/managers/obstacle_spawner.dart (seed support)
   - lib/screens/main_menu.dart (DAILY button)
   - lib/screens/hud_overlay.dart (daily indicator)
   - lib/screens/game_over_screen.dart (mode-aware display)
5. Grep for any TODO or FIXME markers that might have been left behind.

If any issues found, fix them before completing this task.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub passes. C:/flutter/bin/flutter build web --release --no-pub succeeds. No TODO/FIXME markers in new or modified files.</verify>
  <done>Phase 9 builds and analyzes cleanly. All new files exist. All modifications verified. No outstanding issues. Phase 9: Daily Challenge is complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] `C:/flutter/bin/flutter build web --release --no-pub` succeeds
- [ ] GameOverScreen shows "DAILY CHALLENGE" title in daily mode
- [ ] "NEW DAILY BEST!" animation works correctly
- [ ] Daily stats (attempts, streak, daily best) display on game over
- [ ] Endless game over screen unchanged
- [ ] Phase 9 complete — all 5 plans verified
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors or warnings introduced
- Daily challenge game over shows meaningful daily-specific feedback
- Complete daily challenge flow works: menu → DAILY → play → die → game over (daily stats) → retry (same seed) → die → menu
- Phase 9: Daily Challenge complete
</success_criteria>

<output>
After completion, create `.planning/phases/09-daily-challenge/09-05-SUMMARY.md`

Update to include "Phase 9 complete" in Next Step section.
</output>
