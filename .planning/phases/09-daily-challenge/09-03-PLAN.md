---
phase: 09-daily-challenge
plan: 03
type: execute
---

<objective>
Complete the daily challenge game flow by making gameOver, reset, and returnToMenu mode-aware.

Purpose: Ensure daily challenge games record scores to the daily repository, retries use the same seed, and returning to menu resets the mode cleanly.
Output: Mode-aware game lifecycle methods that correctly branch between endless and daily flows.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-daily-challenge/09-01-SUMMARY.md
@.planning/phases/09-daily-challenge/09-02-SUMMARY.md

@lib/game/pulse_game.dart
@lib/utils/daily_challenge_repository.dart
@lib/utils/daily_seed.dart
@lib/game/managers/score_manager.dart
@lib/utils/score_repository.dart

**Tech stack available:** Flutter 3.41.0, shared_preferences ^2.3.0
**Established patterns:**
- gameOver() records scores via ScoreRepository, awards XP, triggers death effects
- resetGame() restarts the game session with full manager resets
- returnToMenu() clears overlays, resets state, shows MainMenu
- Fire-and-forget async for persistence (.then() pattern)
**Constraining decisions:**
- Daily games ALSO count toward lifetime stats (ScoreRepository records all games)
- Daily-specific tracking is ADDITIONAL via DailyChallengeRepository
- resetGame() retry in daily mode must use same seed (todaysSeed())
- returnToMenu() must reset _gameMode to endless
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make gameOver() mode-aware with daily score recording</name>
  <files>lib/game/pulse_game.dart</files>
  <action>In the gameOver() method, AFTER the existing ScoreRepository.recordGameEnd() call (which should remain — daily games count for lifetime stats too), add daily-specific recording:

```dart
// Daily-specific score recording (in addition to lifetime stats above).
if (_gameMode == GameMode.daily) {
  DailyChallengeRepository.instance
      .recordDailyGame(score: scoreManager.displayScore)
      .then((isNewDailyBest) {
    lastDailyWasNewBest = isNewDailyBest;
  });
}
```

Place this block right after the existing `ScoreRepository.instance.recordGameEnd(...)` block (after its .then() handler). The daily recording runs in parallel with the lifetime recording — both are fire-and-forget.

Do NOT change any other logic in gameOver() — death effects, audio, overlays, slow-mo all work the same regardless of mode.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors. Verify gameOver() has both ScoreRepository and DailyChallengeRepository recording paths.</verify>
  <done>gameOver() records daily scores to DailyChallengeRepository when in daily mode, while still recording lifetime stats to ScoreRepository in all modes.</done>
</task>

<task type="auto">
  <name>Task 2: Make resetGame() and returnToMenu() mode-aware</name>
  <files>lib/game/pulse_game.dart</files>
  <action>1. Modify resetGame() to use seeded spawner when retrying in daily mode.
Replace `obstacleSpawner.reset();` with:
```dart
obstacleSpawner.reset(
  seed: _gameMode == GameMode.daily ? todaysSeed() : null,
);
```
Also add `lastDailyWasNewBest = false;` alongside existing `lastRunWasNewBest = false;`.
Do NOT reset _gameMode — retry should stay in the same mode. A player retrying a daily challenge should get the same seeded sequence.

2. Modify returnToMenu() to reset game mode:
Add `_gameMode = GameMode.endless;` early in the method (after existing SFX/stopBgm lines). This ensures returning to the main menu always resets to endless mode — the player must explicitly tap DAILY again to enter daily mode.

All other logic in both methods (audio, player reset, obstacle clearing, overlay management) remains unchanged — mode doesn't affect those mechanics.</action>
  <verify>C:/flutter/bin/flutter analyze --no-pub 2>&1 | tail -5 — no errors. Verify resetGame() passes seed for daily mode. Verify returnToMenu() resets _gameMode to endless.</verify>
  <done>resetGame() retries with same seed in daily mode. returnToMenu() resets to endless mode. Both methods preserve all existing behavior for endless mode.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `C:/flutter/bin/flutter analyze --no-pub` passes with no errors
- [ ] gameOver() records to DailyChallengeRepository when in daily mode
- [ ] gameOver() still records to ScoreRepository in all modes
- [ ] resetGame() uses todaysSeed() when retrying daily mode
- [ ] resetGame() preserves current game mode (doesn't reset it)
- [ ] returnToMenu() resets _gameMode to GameMode.endless
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No analyzer errors or warnings introduced
- Complete daily challenge lifecycle: start → play → die → retry (same seed) → die → menu (mode reset)
</success_criteria>

<output>
After completion, create `.planning/phases/09-daily-challenge/09-03-SUMMARY.md`
</output>
