---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Create the Flutter project with Flame engine dependencies and establish the project directory structure.

Purpose: Get the foundational project scaffolding in place so all subsequent plans have a working, buildable project to build upon.
Output: A Flutter project that builds successfully with Flame dependencies installed and organized directory structure.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flutter project and add Flame dependencies</name>
  <files>pubspec.yaml</files>
  <action>
    Create a Flutter project in the current directory:
    `flutter create --project-name pulse_game .`

    Then add Flame dependencies:
    `flutter pub add flame`
    `flutter pub add flame_audio`

    Verify the project is healthy:
    `flutter pub get`
    `flutter analyze`

    Do NOT pin exact versions — let pub resolve latest compatible.
    Do NOT add flame_bloc or shared_preferences yet — those are for later phases.
  </action>
  <verify>
    `flutter pub get` succeeds.
    `flutter analyze` shows no errors (warnings about default generated code are OK).
    pubspec.yaml contains flame and flame_audio in dependencies.
  </verify>
  <done>Flutter project exists with pubspec.yaml containing flame + flame_audio, `flutter analyze` passes</done>
</task>

<task type="auto">
  <name>Task 2: Set up project directory structure</name>
  <files>
    lib/game/pulse_game.dart,
    lib/game/config/game_config.dart,
    lib/game/components/.gitkeep,
    lib/game/effects/.gitkeep,
    lib/game/managers/.gitkeep,
    lib/screens/.gitkeep,
    lib/utils/.gitkeep,
    assets/audio/.gitkeep,
    assets/images/.gitkeep
  </files>
  <action>
    Create the directory structure from research architecture:

    lib/
    ├── main.dart              (already exists from flutter create)
    ├── game/
    │   ├── pulse_game.dart    (minimal placeholder)
    │   ├── components/        (.gitkeep — player, obstacles in later phases)
    │   ├── effects/           (.gitkeep — screen shake, particles in later phases)
    │   ├── managers/          (.gitkeep — obstacle spawning, scoring in later phases)
    │   └── config/
    │       └── game_config.dart (minimal placeholder)
    ├── screens/               (.gitkeep — menu overlays in later phases)
    └── utils/                 (.gitkeep — audio manager in later phases)
    assets/
    ├── audio/                 (.gitkeep — SFX and music in later phases)
    └── images/                (.gitkeep — sprites in later phases)

    Placeholder pulse_game.dart:
    ```dart
    import 'package:flame/game.dart';

    class PulseGame extends FlameGame {
      // Game implementation added in Plan 01-02
    }
    ```

    Placeholder game_config.dart:
    ```dart
    class GameConfig {
      // Game constants added in Plan 01-05
    }
    ```

    Use .gitkeep files in empty directories so git tracks them.
    Do NOT modify main.dart yet — that's Plan 01-02.
    Do NOT create full implementations — just placeholders.
  </action>
  <verify>
    All directories exist (game/, game/components/, game/effects/, game/managers/, game/config/, screens/, utils/, assets/audio/, assets/images/).
    `flutter analyze` still passes.
  </verify>
  <done>Directory structure matches research architecture, placeholder files exist, project still analyzes clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter pub get` succeeds
- [ ] `flutter analyze` passes
- [ ] flame and flame_audio are in pubspec.yaml dependencies
- [ ] Directory structure matches research architecture
- [ ] Placeholder files exist and compile
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Project builds and analyzes cleanly
- Directory structure ready for subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
