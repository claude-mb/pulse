---
phase: 01-foundation
plan: 02
type: execute
---

<objective>
Integrate Flame's GameWidget with Flutter and create the game loop skeleton with fixed resolution camera.

Purpose: Establish the core game shell that all gameplay systems will be built into. This is the first time we see something on screen.
Output: Running app showing an empty Flame game canvas with correct aspect ratio and a colored background.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
./.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Key files from Plan 01-01:
@lib/game/pulse_game.dart
@lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PulseGame class with fixed resolution camera</name>
  <files>lib/game/pulse_game.dart</files>
  <action>
    Replace the placeholder PulseGame with a proper FlameGame subclass:

    - Extend FlameGame with HasCollisionDetection mixin
    - In onLoad(), set up CameraComponent.withFixedResolution(width: 400, height: 800)
      This creates a virtual 400x800 game world with automatic letterboxing on all devices.
    - Set a dark background color (Color(0xFF1A1A2E) — dark navy) so the game canvas is visible
    - Add a ScreenHitbox to world for future boundary detection
    - Add empty stub methods: startGame(), gameOver(), resetGame() — wired properly in Plan 01-04

    Use HasGameReference<PulseGame> (NOT the deprecated HasGameRef) in any component that needs game access.
    Do NOT add player, obstacles, or any game entities yet — those are Phase 2.
  </action>
  <verify>`flutter analyze` passes with no errors on lib/game/pulse_game.dart</verify>
  <done>PulseGame class has FlameGame + HasCollisionDetection, fixed resolution camera at 400x800, dark background, ScreenHitbox added</done>
</task>

<task type="auto">
  <name>Task 2: Wire main.dart with GameWidget.controlled()</name>
  <files>lib/main.dart</files>
  <action>
    Replace the default Flutter counter app with Flame GameWidget integration:

    - Use GameWidget<PulseGame>.controlled(gameFactory: PulseGame.new) — the modern pattern
    - Register placeholder overlay builders for: 'MainMenu', 'GameOver', 'HUD', 'Pause'
      Each builder returns a simple Container with centered Text showing the overlay name
      (e.g., Container(color: Colors.black54, child: Center(child: Text('Main Menu', style: TextStyle(color: Colors.white, fontSize: 24)))))
    - Set initialActiveOverlays to const ['MainMenu'] so the menu shows on launch
    - Wrap in MaterialApp > Scaffold for proper app structure
    - Set Scaffold backgroundColor to black to match game aesthetic

    Do NOT use the old pattern of passing a game instance — use gameFactory.
    Do NOT create separate widget files for overlays yet — that's Plan 01-04.
  </action>
  <verify>`flutter analyze` passes. `flutter run` launches the app (on connected device or emulator).</verify>
  <done>main.dart uses GameWidget.controlled(), overlay placeholders registered, app launches showing MainMenu text over dark game canvas</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Flame game shell with dark background and MainMenu overlay placeholder</what-built>
  <how-to-verify>
    1. Run: `flutter run` (on Android emulator or connected device)
    2. App should launch showing a dark background (dark navy color)
    3. "Main Menu" text should be visible centered on screen (white text on semi-transparent overlay)
    4. No errors in console
    5. App should fill the screen with proper letterboxing (black bars if aspect ratio doesn't match 1:2)
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze` passes
- [ ] App runs on device/emulator
- [ ] Dark game canvas visible
- [ ] MainMenu overlay text displays
- [ ] No console errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Game canvas displays with fixed resolution
- Overlay system functional (MainMenu visible)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
