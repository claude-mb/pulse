---
phase: 01-foundation
plan: 05
type: execute
---

<objective>
Set up the asset loading pipeline and create game configuration constants for consistent tuning.

Purpose: Establish the infrastructure for loading assets and centralizing game tuning values. This prevents magic numbers scattered through code and prepares for audio/image assets in later phases.
Output: pubspec.yaml configured for asset loading, game_config.dart with all foundation-level constants, asset pipeline verified.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

# Key files from prior plans:
@lib/game/pulse_game.dart
@lib/game/config/game_config.dart
@lib/game/components/tap_indicator.dart
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure asset directories and pubspec.yaml</name>
  <files>pubspec.yaml</files>
  <action>
    1. Update pubspec.yaml to register asset directories:
       ```yaml
       flutter:
         assets:
           - assets/audio/
           - assets/images/
       ```

    2. Ensure the asset directories exist (should already from Plan 01-01).
       Add a placeholder .gitkeep in each if empty (Flutter requires directories to exist when declared).

    3. Verify `flutter pub get` still succeeds with asset directories declared.

    Note: Flame loads assets from the assets/ directory structure.
    flame_audio expects audio files in assets/audio/.
    Images (if used) go in assets/images/.

    Do NOT add actual audio or image files yet — those are Phase 4 (visual) and Phase 5 (audio).
    The pipeline just needs to be configured and ready.
  </action>
  <verify>
    `flutter pub get` succeeds.
    pubspec.yaml has assets section with audio/ and images/ directories.
  </verify>
  <done>pubspec.yaml declares asset directories, pipeline ready for future asset additions</done>
</task>

<task type="auto">
  <name>Task 2: Create game configuration constants</name>
  <files>lib/game/config/game_config.dart, lib/game/pulse_game.dart, lib/game/components/tap_indicator.dart</files>
  <action>
    1. Replace the placeholder game_config.dart with comprehensive configuration:

    ```dart
    import 'dart:ui';

    class GameConfig {
      // Prevent instantiation
      GameConfig._();

      // Game world dimensions (virtual resolution)
      static const double worldWidth = 400;
      static const double worldHeight = 800;

      // Colors
      static const Color backgroundColor = Color(0xFF1A1A2E);
      static const Color playerColor = Color(0xFFE94560);
      static const Color obstacleColor = Color(0xFF0F3460);
      static const Color accentColor = Color(0xFF16213E);
      static const Color textColor = Color(0xFFFFFFFF);

      // Tap indicator
      static const double tapIndicatorRadius = 15.0;
      static const double tapIndicatorDuration = 0.3;

      // Player (placeholder values — tuned in Phase 2)
      static const double playerSize = 40.0;
      static const double playerSpeed = 300.0;

      // Obstacles (placeholder values — tuned in Phase 2-3)
      static const double obstacleSpeed = 200.0;
      static const double spawnInterval = 1.5;
    }
    ```

    2. Update PulseGame to use GameConfig values:
       - Replace hardcoded camera resolution with GameConfig.worldWidth / GameConfig.worldHeight
       - Replace hardcoded background color with GameConfig.backgroundColor

    3. Update TapIndicator to use GameConfig values:
       - Replace hardcoded radius with GameConfig.tapIndicatorRadius
       - Replace hardcoded duration with GameConfig.tapIndicatorDuration

    Centralizing these values now means Phase 2+ can tune gameplay by changing one file.
    Do NOT add values for features that don't exist yet beyond the placeholders above.
  </action>
  <verify>
    `flutter analyze` passes.
    PulseGame and TapIndicator reference GameConfig instead of hardcoded values.
    No magic numbers remain in pulse_game.dart or tap_indicator.dart.
  </verify>
  <done>GameConfig has all foundation constants, PulseGame and TapIndicator use GameConfig values, no hardcoded magic numbers</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze` passes
- [ ] `flutter run` still works correctly with centralized config values
- [ ] pubspec.yaml has asset directories configured
- [ ] GameConfig contains all foundation-level constants
- [ ] No hardcoded magic numbers in game code
- [ ] Phase 1 complete — all 5 plans done
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Asset pipeline configured and ready for future assets
- GameConfig centralizes all tuning values
- Phase 1: Foundation complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`:
This is the final plan for Phase 1. Success criteria includes "Phase 1 complete".
</output>
